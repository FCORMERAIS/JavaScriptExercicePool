const indexOf = (arr,n) =>  {
    for (let index = 0; index < arr.length; index++) {
        if (n === arr[index]) {
            return index
        }
    }
}

const lastIndexOf  = (arr,n) => {

}

const includes  = (arr,n) => {
    
}